{
  "devDependencies": {
    "cypress": "^3.6.1",
    "start-server-and-test": "^1.10.6"
  },
  "scripts": {
    "start": "hugo server",
    "cy:run": "cypress run",
    "test": "start-server-and-test start http://localhost:1313 cy:run"
  }
}
