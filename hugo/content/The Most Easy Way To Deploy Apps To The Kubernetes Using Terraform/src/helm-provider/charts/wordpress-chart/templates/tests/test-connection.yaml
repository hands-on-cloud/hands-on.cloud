apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wordpress-chart.fullname" . }}-test-connection"
  labels:
    {{- include "wordpress-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wordpress-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
